# Rocky

This repository is organized by platform:

- `windows/` contains the existing Windows update assets and version metadata.
- `macos/` contains the macOS Swift/Xcode project and `macos/version.json` for in-app update metadata.

Open the macOS project from:

```sh
open macos/Rocky.xcodeproj
```
